---
- name: Getting ngrok
  unarchive:
    src: "{{ ngrok_download_url }}"
    dest: "{{ ngrok_install_dir }}"
    mode: "u=rwx,g=rx,o=rx"
    owner: "{{ ngrok_bin_owner }}"
    group: "{{ ngrok_bin_group }}"
    creates: "{{ ngrok_install_dir }}"
